name: Policy Gate

on:
  workflow_dispatch:

jobs:
  policy-gate-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: |
          echo "Policy gate skeleton."
          echo "Gate logic will be added after scan reports exist."
