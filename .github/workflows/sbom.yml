name: SBOM

on:
  workflow_dispatch:

jobs:
  sbom-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: |
          echo "SBOM workflow skeleton."
          echo "Syft/CycloneDX generation will be added later."
